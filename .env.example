# The Discord channel webhook URL that receives the embeds.
# Create one in Discord: Server Settings > Integrations > Webhooks > New Webhook.
DISCORD_WEBHOOK=

# A secret key Roblox must send with every request (x-api-key header).
# Generate a strong random value, e.g.: openssl rand -hex 32
API_KEY=
